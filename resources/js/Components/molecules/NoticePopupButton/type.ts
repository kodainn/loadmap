export type NoticePopupButtonProps = {
    onClick: () => void,
    size: "small" | "medium" | "large"
}

export type ComponentProps = {
    onClick: () => void,
    size: "small" | "medium" | "large"
}